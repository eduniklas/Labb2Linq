@model LinqSchool.Models.ActiveCourse

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Student</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Students.FirstName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Students.FirstName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Students.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Students.LastName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Students.Class.ClassName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Students.Class.ClassName)
        </dd>
            <dt>
            @Html.DisplayNameFor(model => model.Courses)
        </dt>
        <dd>
            <table class="table">
                <tr>
                    <th>Course</th>
                    <th>Teacher</th>
                </tr>
                @*
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Courses.CourseName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Courses.Teachers.FirstName)
                            @Html.DisplayFor(modelItem => item.Courses.Teachers.LastName)
                        </td>
                    </tr>
                }
               *@
            </table>
        </dd>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Students.StudentId">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
    </dl>
</div>
